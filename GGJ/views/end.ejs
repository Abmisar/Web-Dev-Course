<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
  <meta charset="UTF-8">
  <title>نهاية اللعبة</title>
  <link rel="stylesheet" href="/styles/main.css">
</head>
<body>
  <h1>انتهت اللعبة بعد ليلتين 🎉</h1>
  
  <div class="final-roles">
    <% players.forEach(p => { %>
      <div class="role-card <%= p.role === 'المجرم' ? 'criminal' : 'citizen' %>">
        <h3><%= p.name %></h3>
        <p class="role"><%= p.role %></p>
        <p class="status"><%= p.alive ? 'حي' : 'ميت' %></p>
      </div>
    <% }) %>
  </div>

  <form action="/reset" method="POST">
    <button class="btn">لعبة جديدة</button>
  </form>
</body>
</html>