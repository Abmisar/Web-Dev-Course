<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
  <meta charset="UTF-8">
  <title>يوم اللعبة</title>
  <link rel="stylesheet" href="/styles/main.css">
  <style>
    body {
      background-color: #0f172a;
      color: white;
      font-family: 'Tajawal', sans-serif;
    }
    .container {
      max-width: 800px;
      margin: 0 auto;
      padding: 20px;
    }
    h1 {
      color: #fbbf24;
      text-align: center;
    }
    .player-card {
      background: #1e293b;
      border-radius: 8px;
      padding: 15px;
      margin: 10px 0;
    }
    .alive {
      border-left: 5px solid #4ade80;
    }
    .dead {
      border-left: 5px solid #ef4444;
      opacity: 0.7;
    }
  </style>
</head>
<body>
  <div class="container">
    <h1><%= result %></h1>
    
    <h2>اللاعبون:</h2>
    <% players.forEach(player => { %>
      <div class="player-card <%= player.alive ? 'alive' : 'dead' %>">
        <h3><%= player.name %></h3>
        <p>الدور: <%= player.role %></p>
        <p>الحالة: <%= player.alive ? 'حي' : 'ميت' %></p>
      </div>
    <% }) %>

    <% if (isFinalDay) { %>
      <form action="/end" method="GET">
        <button type="submit" class="btn">إنهاء اللعبة</button>
      </form>
    <% } else { %>
      <form action="/night" method="GET">
        <input type="hidden" name="night" value="<%= night + 1 %>">
        <button type="submit" class="btn">الانتقال إلى الليل التالي</button>
      </form>
    <% } %>
  </div>
</body>
</html>